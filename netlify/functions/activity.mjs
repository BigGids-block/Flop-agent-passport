import { DID_PATTERN, verifyContribution } from '../../lib/verification.mjs';
function json(body, status = 200) { return new Response(JSON.stringify(body), { status, headers: { 'Content-Type': 'application/json; charset=utf-8', 'Cache-Control': 'no-store' } }); }
export default async (request) => { if (request.method !== 'GET') return json({ error: 'Method not allowed.' }, 405); const params = new URL(request.url).searchParams; const did = params.get('did') || ''; const url = params.get('url') || ''; if (!DID_PATTERN.test(did)) return json({ error: 'A valid did:key is required.' }, 400); if (!url) return json({ error: 'A public contribution URL is required.' }, 400); try { return json(await verifyContribution(did, url)); } catch (error) { return json({ error: error.message || 'The contribution record could not be checked.' }, 400); } };
